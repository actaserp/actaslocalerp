# actas
server.port=8030

# actas
server.servlet.session.cookie.name=mes21_jsessionid

# actas
spring.datasource.hikari.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.jdbc-url=${MES_DB_URL}

spring.datasource.hikari.username=${MES_DB_USERNAME}
spring.datasource.hikari.password=${MES_DB_PASSWORD}
spring.datasource.hikari.maximum-pool-size = 5
spring.datasource.hikari.minimum-idle = 1
spring.datasource.hikari.maxLifetime = 1800000
spring.datasource.hikari.idle-timeout = 600000
spring.datasource.hikari.leak-detection-threshold=5000

# 1. ??? ?? ?? ?: ?? ??? ???? localhost(127.0.0.1)? ??
# 2. ?? ?? ?: docker-compose? environment ?? ???? my-redis(?????)? ??

spring.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.redis.port=${SPRING_DATA_REDIS_PORT:6379}

# 2.7 ?? ???? ?? ?? ? (?? ??)
spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}